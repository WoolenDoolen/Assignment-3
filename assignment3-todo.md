# Assignment 3 Remaining Work

Use this to keep later commits meaningful instead of accidentally finishing everything today.

- Build the reward screen flow that previews a generated spell after each wave.
- Let the player take the reward spell and replace/drop one of up to four equipped spells.
- Update the spell slot UI so all equipped spells can be shown and selected.
- Playtest modifier combinations in Unity, especially splitter plus doubler and on-hit bursts.
- Write the Assignment 3 report and export it as `report.pdf`.
- Create the final `submission.json` and zip exactly `submission.json` plus `report.pdf`.
